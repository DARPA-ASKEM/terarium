{
  "name": "shared",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "npx eslint . --fix",
    "prettier": "npx prettier './**/*.ts' --write",
    "format": "yarn lint && yarn prettier"
  }
}
