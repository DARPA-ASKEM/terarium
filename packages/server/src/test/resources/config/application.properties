# To run tests locally, point to your local database.
# TODO: eventually we should have different profiles for local and CI testing, or the same datasource for both.

# Local database
# spring.datasource.url=jdbc:postgresql://localhost:5433/terarium

# CI database. DO NOT COMMIT THIS LINE UNCOMMENTED.
spring.datasource.url=jdbc:postgresql://localhost:5432/panteratest
spring.datasource.password=postgres
spring.datasource.username=postgres
