{
  "description": "For adding an indextime to all documents",
  "version": 2,
  "processors": [
    {
      "set": {
        "field": "feature.indextime",
        "value": "{{_ingest.timestamp}}"
      }
    }
  ]
}
