import { ModelCouplingOperation as operation } from './model-coupling-operation';
import node from './tera-model-coupling-node.vue';
import drilldown from './tera-model-coupling.vue';

const name = operation.name;

export { name, operation, node, drilldown };
