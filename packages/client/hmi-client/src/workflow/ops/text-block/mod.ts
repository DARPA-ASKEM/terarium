import { TextBlockOperation as operation } from './text-block';
import node from './tera-text-block.vue';

const name = operation.name;

// export { name, operation, node, drilldown };
export { name, operation, node };
