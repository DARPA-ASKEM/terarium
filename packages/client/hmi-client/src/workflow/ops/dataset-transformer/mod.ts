import { DatasetTransformerOperation as operation } from './dataset-transformer-operation';
import node from './tera-dataset-transformer-node.vue';
import drilldown from './tera-dataset-transformer.vue';

const name = operation.name;

export { name, operation, node, drilldown };
