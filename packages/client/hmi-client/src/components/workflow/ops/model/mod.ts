import { ModelOperation as operation } from './model-operation';
import node from './tera-model-node.vue';
import drilldown from './tera-model-drilldown.vue';

const name = operation.name;

export { name, operation, node, drilldown };
