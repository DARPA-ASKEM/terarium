from setuptools import setup, find_packages

setup(
    name="taskrunner",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    py_modules=['taskrunner'],
    python_requires=">=3.8",
)
