{
  "name": "terarium",
  "packageManager": "yarn@3.2.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --parallel --interlaced --verbose --topological run build",
    "dev": "yarn workspaces foreach --parallel --interlaced --verbose run dev",
    "format": "yarn workspaces foreach --parallel --interlaced --verbose run format"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.vue": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@vue/eslint-config-airbnb-with-typescript": "^7.0.0",
    "eslint": "^8.23.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-vue": "^9.4.0",
    "husky": "^8.0.1",
    "prettier": "^2.7.1"
  }
}
